import { Point } from "./Point";

export interface PlaneteInterface {
    ajusterPosition(position: Point): Point; // Ajout de la méthode ajusterPosition
  //  EstLibre(position: Point): boolean; besoin pour toute a lheue
}
